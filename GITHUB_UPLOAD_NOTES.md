# GitHub upload notes

Upload the contents of this folder as the root of a private GitHub repository.

Do not upload generated runtime folders from the working copy, especially:

- reports/
- sources/
- server.log

After upload, connect the repository to Render. Render can read render.yaml and start the app with python server.py.
