services:
  - type: web
    name: weekly-report-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python render_runner.py
    autoDeploy: true
    healthCheckPath: /healthz
